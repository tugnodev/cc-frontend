import { BackendFetch } from "$lib/backend";
import { articles } from "../../store/articles";

//export const load = async () => {
//  const fetch = new BackendFetch();
//  const data = await fetch.get("/articles/all");
//  console.log(`res: ${JSON.stringify(data)}`);
//};
